# מסירה לשיחה הבאה — AnyDay, 5 בספטמבר 2026

## מה נסגר היום: הבורר + הכיסוי ✅

**`2063f50` — הוויזרד מציג את המועמדות במקום להכריע לבד, והכיסוי נאמר לפני הבנייה.**
נדחף ל-`fork` (`feat/bi`). 290 טסטים עוברים (היו 268), `tsc`/`build` נקיים,
lint ללא שגיאות חדשות (הבסיס נשאר 17).

מה שנבנה:

| קובץ | מה |
|---|---|
| `src/lib/board-profile.ts` | `columnCandidates` — אותה ראיה ש-`canonicalColumn` הכריעה עליה, כרשימה. שתיהן חולקות דירוג אחד (`rankSpellings`), כך שהמסומן־מראש הוא בדיוק מה שהמנוע היה בוחר |
| `src/lib/column-coverage.ts` | האריתמטיקה והמשפטים: `summarizeFill`, `candidateFacts`, `fillLine` — טהור, נבדק בלי טוקן |
| `src/lib/board-fetch.ts` | `fetchColumnFill` — קורא **עמודה אחת** מכל לוח, לא את הלוחות |
| `src/app/api/column-coverage/route.ts` | מסלול חדש; עד 6 מועמדות, עד 20 קריאות־לוח לבקשה |
| `src/components/live/CandidatePicker.tsx` | המסך |
| `src/lib/cross-board.ts` | `CROSS_BOARD_MAX` — התקרה של 10 לוחות הייתה מספר קבוע בשלושה מקומות |

שלוש הכרעות שכדאי לדעת עליהן לפני שנוגעים:

1. **המספר שליד שם = כמה לוחות קוראים לעמודה שלהם ככה** — לא כמה לוחות השם
   "מגיע" אליהם. המדד ההוא תגמל עמימות (בגללו `היום` ניצחה את
   `מה עושה היום`), והעברתו למסך רק הייתה מעבירה את העמימות הלאה. איות שונה
   נספר בנפרד ("בשם דומה") ולא נכנס לדשבורד.
2. **נמדדים בדיוק הלוחות שמהם ייבנה הדשבורד** (10 הראשונים), כדי שהאחוז שמוצג
   יהיה האחוז של מה שייווצר. כשיש יותר מ-10, המסך אומר "10 מתוך 11" ונוקב
   בשם הלוח שלא ייכלל — במקום שייעלם בשקט בשמירה.
3. **כל מה שלא נמדד נאמר באותה שורה של האחוז**, לא אחריה.

🔴 **מה שטרם אומת:** המדידה מול Monday אמיתי לא רצה — ההרשאה שניתנה היא
**שמות לוחות ועמודות בלבד**. הקוד נבדק ברמת הנתונים (מקצה לקצה, על המספרים
האמיתיים של מיטל) והמסך צולם מרונדר, אבל `fetchColumnFill` עוד לא פגש את ה-API.
בקריאה הראשונה שווה לוודא ש-`column_values(ids:)` מתקבל; יש נפילה־לאחור שקוראת
את כל תאי השורה אם הארגומנט נדחה.

🔴 **טרם נפרס.** `npx vercel --prod --yes` מתוך `C:\Users\meyta\Downloads\anyday`
— פריסה ידנית בלבד, ורק באישור מיטל.

---

## המשימה הבאה

**קטלוג עמודות מתמיד לארגון** — טבלה לוח→עמודות→סוג→מלאות שמתרעננת ברקע.
היא נדחתה במכוון היום כי 1+2 עובדים גם בלעדיה, אבל עכשיו יש לה שני צרכנים
ממשיים: היא תהפוך את הבורר למיידי (במקום שאילתת 100 לוחות בכל הצעה) ואת
הכיסוי לידוע מראש (במקום עד 20 קריאות־לוח בכל פתיחת ויזרד).

**נדחה במכוון, ועדיין:** לוותר על טקסט חופשי לטובת בורר בלבד — זה יעבוד תמיד,
ויהפוך את AnyDay לעוד כלי BI שצריך ללמוד.

---

## מצב הפרויקט

- **ענף:** `feat/bi` על ה-fork `meytalp-dev/anyday` (ה-`origin` הוא של לירון, אין
  לנו הרשאת דחיפה אליו — דוחפים ל-`fork`).
- **CI:** `.github/workflows/ci.yml` — tsc + טסטים + build על כל push. ירוק.
  🔴 **lint לא נכלל בכוונה** — `eslint .` מדווח 17 שגיאות שקדמו. CI אדום ביום
  הראשון = CI שמתעלמים ממנו. להכניס אחרי שינוקו.
- **פריסה ידנית בלבד:** `npx vercel --prod --yes`. push ל-git **לא** מעדכן את
  האתר. אין `VERCEL_TOKEN` (רק מיטל יכולה לייצר).
- **בדיקות:** 290 עוברות. `npx tsc --noEmit` ו-`npm run build` נקיים.
  🔴 **על המכונה הזו `vitest run` (pool=forks) מפיל workers ומדווח ירוק על פחות
  קבצים ממה שרץ** — 19 קבצים במקום 21. להריץ `npx vitest run --pool=threads`.
  זה בדיוק הלקח השלישי בלבוש אחר: ירוק שלא ספר את הכול.
- **סכימות Supabase:** v6 ✅ v7 ✅ v8 ✅ — כולן הורצו ואומתו.

## עובדות שחוסכות שעה

- **בעלוּת:** Vercel ו-Supabase = של מיטל (`meytalp-1185`). **חשבון ה-Monday
  המחובר הוא `golanliron1's Team` — של לירון.** Cloudflare (הדומיין) — כנראה גם.
- 🔴 **הרשאה שניתנה, ולא לחרוג ממנה:** קריאת **שמות לוחות ועמודות בלבד**
  מה-Monday של לירון. אפס שורות, אפס כתיבה. כל קריאה רחבה יותר — לבקש שוב.
- **לאמת מסלולים בלי לשלוח מייל לאף אחד:** `POST /auth/v1/admin/generate_link`
  עם מפתח השירות מחזיר קישור-קסם ישירות; הולכים איתו ב-Playwright. **תמיד
  למחוק אחרי** (`DELETE /auth/v1/admin/users/<id>` + מחיקת הארגון).
- **Playwright:** לא מותקן בפרויקט. משתמשים במטמון ה-npx:
  `file:///C:/Users/meyta/AppData/Local/npm-cache/_npx/705bc6b22212b352/node_modules/playwright/index.mjs`
- **לראות רכיב React בלי להריץ את האפליקציה** (כך אומת הבורר): לארוז אותו עם
  esbuild (`--alias:@=./src`, פלט **לתוך** `node_modules/.cache/` כדי ש-react
  ייפתר), לרנדר עם `renderToStaticMarkup`, ולצלם את ה-HTML ב-Playwright. חוסך
  התחברות, Monday, והכול — ותפס שני ליקויים שהקומפילציה לא רואה.
- **לוגים אמיתיים:** `npx vercel logs anyday-opal.vercel.app --json`.

## ארבעה לקחים — קראו לפני שמתקנים משהו

1. 🔴 **אותו סימפטום פעם שלישית = להביא נתונים, לא לתקן שכבה רביעית.** שלושת
   התיקונים הראשונים היו כולם נכונים וכולם חסרי טעם, כי כולם נשענו על ניחוש
   איך העמודה נקראת. שאילתת מטא-דאטה אחת פתרה מה ששלוש שעות תיקונים לא.
2. 🔴 **כשבוחרים מדד — לשאול מה הוא מתגמל בקצה.** דירגתי לפי "לכמה לוחות
   הכותרת מתאימה". מילה קצרה מוכלת בתוך כל כותרת ארוכה, ולכן `היום` קיבלה 17
   ו-`מה עושה היום` קיבלה 13. **מקסום reach = מקסום עמימות.**
3. 🔴 **טסט ירוק שמקודד באג גרוע מהיעדר טסט.** כשכותבים טסט לפונקציה חדשה:
   **מי צורך את הפלט ולמה.** (וראו למעלה: גם runner שמדווח ירוק על פחות קבצים
   ממה שרץ הוא אותו כשל.)
4. 🔴 **תנאי גורף על השלם מסתיר כשל בחלק.** **כשמשהו נבנה חלקית — לומר מה לא
   נבנה.** זה הפך היום לכלל עיצוב: כל מספר במסך החדש נושא לידו את מה שהוא לא
   מכסה.

## פתוח, לא קשור למשימה

🔴 **רק מיטל יכולה (Cloudflare):** `anyday.co.il` מחזיר **שגיאת SSL 526** —
הדומיין מעולם לא חובר ל-Vercel (`vercel domains ls` = 0). אין **MX** (המייל
`hello@anyday.co.il` מת, והוא הכתובת בשני המסמכים המשפטיים), ואין **SPF/DKIM**.

🟠 שאר הרשימה: סקירת עו״ד לתנאים ולמדיניות הפרטיות · מסלול מפיילוט לתשלום ·
ניקוי 17 שגיאות ה-lint והכנסת lint ל-CI · אריחי ה-KPI נראים חצי-ריקים (גרוע
יותר בנייד) · אין `VERCEL_TOKEN` לפריסה אוטומטית.
